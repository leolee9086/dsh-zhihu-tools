# 计划：浏览器插件授权方案（参考 wechatsync）

> **状态：计划阶段，尚未实现。** 本文只描述设计与实施步骤。
> 目的：让「发布到知乎」的授权**可信且保密** —— cookie 永远不离开浏览器。

---

## 一、为什么需要这条路

现有两条路各自的短板：

| 路线 | 短板 |
|---|---|
| 官方 Publish OpenAPI | 需要内测的 `APP_SECRET`，且凭证以文件形式落在本机 |
| 网页会话（session） | 要把 cookie **交给插件进程**：会话凭据从浏览器流到了 Node 侧，多一层暴露面，也让"谁能读到它"这个问题变复杂 |

**wechatsync 的做法**（`@wechatsync/cli` + 浏览器扩展）值得直接借鉴：

- CLI 只做两件事：起一个**本地 WebSocket**（默认端口 9527）、把"要发什么"递过去；
- **真正的平台 API 调用由浏览器扩展完成**，用的就是浏览器本来就有的登录态；
- **cookie 从不出浏览器**，CLI 只拿得到结果。

这样信任边界收敛到浏览器扩展，插件进程不再持有会话凭据。第三方登录本身没有问题，这条路解决的是**凭据流转**问题。

---

## 二、架构

```
┌──────────────────────────┐        本地 WebSocket          ┌─────────────────────────┐
│  dsh-zhihu-tools (宿主)   │  ws://127.0.0.1:<port>        │  浏览器扩展              │
│                          │ ◄────────────────────────────► │  (Edge / Chrome)        │
│  · zhihu_publish_article │        token 认证              │                         │
│  · 转发发布请求           │                                │  · fetch(credentials:   │
│  · 拿回结果              │                                │      'include')         │
└──────────────────────────┘                                │  · 浏览器自动带 cookie   │
                                                            └───────────┬─────────────┘
                                                                        │ HTTPS
                                                                        ▼
                                                          ┌─────────────────────────┐
                                                          │ zhuanlan.zhihu.com      │
                                                          │ www.zhihu.com           │
                                                          └─────────────────────────┘
```

**关键点**：扩展侧用 `fetch(url, { credentials: 'include' })` 发请求，**cookie 由浏览器自动附加**，
扩展代码里从头到尾**不出现 cookie 的值** —— 连扩展自己都读不到它。

---

## 三、协议草案

宿主 → 扩展：

```json
{ "id": "req-1", "type": "publish.article",
  "payload": { "title": "…", "html": "<p>…</p>", "commentPermission": "anyone", "topics": [] } }
```

扩展 → 宿主：

```json
{ "id": "req-1", "type": "publish.result",
  "payload": { "ok": true, "url": "https://zhuanlan.zhihu.com/p/123456", "articleId": "123456" } }
```

其它消息：

| type | 方向 | 用途 |
|---|---|---|
| `hello` | 扩展 → 宿主 | 握手，带扩展版本与 token |
| `auth.status` | 宿主 → 扩展 | 查 `z_c0` 是否有效（扩展只需回答"已登录/未登录"，不回传 cookie） |
| `ping` / `pong` | 双向 | 保活与探测 |

---

## 四、扩展侧要做的事

1. `manifest.json`：`host_permissions` 覆盖 `https://zhuanlan.zhihu.com/*`、`https://www.zhihu.com/*`；
   `background` 用 service worker 维持到宿主的连接。
2. 连接与重连：向 `ws://127.0.0.1:<port>` 建连，带 token；断开后指数退避重连。
3. 发布三步（与现在 session 路一致，只是改在浏览器里发）：
   `POST /api/articles/drafts` → `PATCH /api/articles/{id}/draft` → `PUT /api/articles/{id}/publish`。
4. **不要**用 `chrome.cookies` 去读 cookie 再转发 —— 那等于把凭据又搬出浏览器一次，白做了这条路。

---

## 五、宿主侧要做的事

1. 新增本地 WebSocket 服务（`webServer` 之外单起，仅绑 `127.0.0.1`），端口可配置。
2. 连接鉴权：一次性 token，写在扩展配置里；宿主只接受携带正确 token 的连接（wechatsync 的 `WECHATSYNC_TOKEN` 同款思路）。
3. `zhihu_publish_article` 增加 `auth_mode: "extension"`；`auto` 的优先级建议改为
   **extension > openapi > session**（最安全的那条优先）。
4. 转发与超时：请求带 `id`，等 `publish.result`；超时则报"扩展未响应"，并提示检查扩展是否在运行。
5. 发布流水与限额照旧（`notePublish` 记账），**与走哪条授权无关**。
6. `tools/pre-execute` 的用户授权关保持不变 —— 这条路只是换了"谁来发"，没有放松审批。

---

## 六、安全设计要点

| 关注点 | 设计 |
|---|---|
| cookie 保密 | 不出浏览器；扩展用 `credentials:'include'` 让浏览器自动附加 |
| 本地端口暴露 | 只绑 `127.0.0.1`；token 认证；拒绝来源不明的连接 |
| 扩展被冒用 | 扩展侧校验宿主端口与 token；宿主侧校验 token 与协议版本 |
| 重放 | 每个请求带唯一 `id`，宿主对已处理的 id 去重 |
| 审批 | 仍然由 `tools/pre-execute` 交给用户裁决，不因换路而豁免 |
| 日志 | 协议消息里**不含** cookie、APP_SECRET、X-Sign |

---

## 七、与现有两条授权的关系

最终形态是三条路并存，由 `auth_mode` 选择：

| auth_mode | 凭据在哪 | 适用 |
|---|---|---|
| `extension` | **只在浏览器里** | 最安全，推荐默认 |
| `openapi` | 本机文件 / 环境变量 | 申请到内测密钥后，最省事 |
| `session` | 插件进程内存 + 平台凭据存储 | 兜底，无需装扩展 |

---

## 八、分阶段实施

1. **阶段一 · 握手**：协议定稿 + 扩展骨架（能连上、能 `ping/pong`、token 校验通过）。
2. **阶段二 · 打通发布**：扩展侧实现三步发布，宿主侧 `auth_mode: "extension"` 接上，端到端发一篇测试文章。
3. **阶段三 · 状态与媒体**：`auth.status` 查询、图片上传（`api.zhihu.com/images`，同样在扩展里发）。
4. **阶段四 · 多平台**：若要把这套扩成 wechatsync 那样的多平台分发，协议里加 `platform` 字段即可。

---

## 九、待确认

- 扩展的安装方式：本地加载（unpacked）还是上架 Edge/Chrome 商店？
- 端口策略：固定 9527（可能撞车）还是宿主协商后告知扩展？
- 是否需要同时支持 Edge 与 Chrome（本机是 Edge，`Profile 1` 已装「文章同步助手」可作参考实现）。
- 与「文章同步助手」扩展共存时如何避免互相抢连接。
