# 方案调研：怎么让我能回答知乎问题

- 时间：2026-09-22
- 起因：哥哥给了 `https://www.zhihu.com/question/2085604034031925212`，要我回答它
- **结论：卡点是授权，不是接口。推荐走浏览器扩展那条路（就是 2026-09-18 那份计划）。**

## 一、问题在哪

要发**回答**，比发**文章**多一道坎：

| | 文章 | 回答 |
|---|---|---|
| 域名 | `zhuanlan.zhihu.com` | `www.zhihu.com` |
| 鉴权 | cookie（`z_c0`+`_xsrf`）+ `x-xsrftoken` | 同上 **+ zse96 v2 签名** |
| 插件现状 | 有工具，**没凭证** | **连工具都没有** |

而且卡在授权这件事**四天前就存在**——见 `D:\dev\zhihu-plugin\计划-发布工具收markdown.md` 第 100-104 行：
那篇 dsh-tool-gateway 的知乎稿写好了，**发布卡在授权上**（OpenAPI 缺 APP_KEY/APP_SECRET，网页会话也没登录）。

实测确认：
- `.credentials.yaml` 里只有读取用的 `ZHIHU_ACCESS_SECRET`，**没有任何发布凭证**
- `~/.zhihu/openapi-credentials.json` 不存在
- 未登录抓问题页返回 **403**（`zh-zse-ck` 反爬挑战）

## 二、第三方客户端的调研结论

哥哥提示去 GitHub 找，同时提醒注意维护情况。逐个查过：

| 项目 | 最后提交 | 结论 |
|---|---|---|
| `DayDreammy/zhihu-auto-answer` | 2026-03-05 | 纯 DOM 自动化（点"写回答"按钮、找 `.ProseMirror` 编辑器、键盘输入），只做到**存草稿**。知乎一改版就废 → **不参考** |
| `ylw1997/zhihu-api` | 2026-04-27 | 只是**只读**接口文档 + 投票/关注四个写操作，**没有发布回答**；x-zse-96 签名脚本是 5 个月前的 → **只能借签名** |
| **`zly2006/zhihu-plus-plus`** | **2026-09-21（昨天）** | 活跃维护。有**当前有效的 zse96 v2 签名**（Kotlin `signFetchRequest` + `rs-zse-sign` Rust crate）；但它是**阅读增强客户端**，只做投票/关注/已读，**不发布回答** → **借签名，不借发布** |

**关键结论：没有一个现成的「发布回答」实现可以直接抄。**

## 三、三个方案

### A. 浏览器扩展授权（2026-09-18 那份计划）

扩展跑在浏览器里，用 `fetch(url, { credentials: 'include' })`，**cookie 从不出浏览器**；
真正的平台调用由浏览器页面完成，**签名由浏览器自己的 JS 处理**。

- 一次解决**所有**写操作：文章、回答、以后评论/点赞
- **天然绕开 zse96 签名维护**——这是它最大的价值：签名算法变了也不用我们跟
- 参考实现：本机 Edge Profile 1 已装的「文章同步助手」
- 代价：要写扩展骨架 + 本地 WebSocket + 发布链路（计划里分了四个阶段）

### B. 插件加 `zhihu_publish_answer`

复用现成的 `zhFetch`、markdown→HTML（lute）、审批守卫（`tools/pre-execute`）、限流流水，
新增一个会话发布函数调 `POST https://www.zhihu.com/api/v4/questions/{qid}/answers`。

- 需要：cookie（扫码登录）+ **zse96 签名**（从知乎++ 的 `rs-zse-sign` 移植）
- 代价：签名算法跟着知乎变，要长期维护

### C. 浏览器直接操作（最快，最脆）

打开 `https://www.zhihu.com/question/{qid}/write`，在富文本编辑器里输入，发布。

- 最快落地，今天就能用
- 脆：富文本注入麻烦，知乎改版就废
- 前提：浏览器桥可用（当前审批超时）

## 四、推荐

**A 是正解**，理由就是那条：它能一次性消掉 zse96 签名的长期维护负担，
而且同样一套协议以后加 `platform` 字段就能扩成多平台分发。

**但为了先回答上那个问题**，C 是最短路径（前提是浏览器桥能通）。

## 五、还没做的

1. **那个知乎问题的内容还没看到**——未登录抓取 403，浏览器桥审批超时。
2. 浏览器桥是否可用，没验证。
3. 方案 A 的实施没开始（计划文档停在设计阶段）。
